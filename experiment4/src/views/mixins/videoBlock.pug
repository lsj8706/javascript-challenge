mixin movieBlock(movie = {})
    a(href=`/${movie.id}`)
        h3=movie.title



mixin detailBlock(movie={}) 
    h1=movie.title 
    p=movie.summary
    each item in movie.genres 
        ul 
            li=item